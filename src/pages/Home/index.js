import "./StyleHome.css"

const Home = ({show}) => {
  return (
    <div className={`container ${show ? "show" : ""}`}>

    <div className='container-home'>
      ssssssss
    </div>
    </div>
  )
}

export default Home